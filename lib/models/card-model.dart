class Mycard {
  Mycard(this.icon, this.title, this.isActive);

  final icon;
  bool isActive = false;
  final title;
}

class Product {
  Product(this.image, this.name, this.price);

  final image;
  final name;
  final price;
}
